/* Phaser Offline Runtime Adapter for this educational MVP. API surface intentionally minimal. */
window.Phaser={VERSION:'3-offline-adapter',AUTO:0,Game:class{constructor(config){this.config=config;requestAnimationFrame(()=>config.start(this));}}};
